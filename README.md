# Arasaka Linguistic Service

Интеллектуальная система для ответов на вопросы с использованием семантического поиска и векторных эмбеддингов. Микросервисная архитектура с MAX ботом и ML сервисом на gRPC.

---

## 📋 Содержание

- [О проекте](#-о-проекте)
- [Архитектура](#-архитектура)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [Разработка](#-разработка)
- [Troubleshooting](#-troubleshooting)

---

## 🎯 О проекте

**Arasaka** - это система для автоматизированных ответов на вопросы, состоящая из двух микросервисов:

- **MAX Bot** - бот для мессенджера MAX, принимающий вопросы от пользователей
- **ML Service** - gRPC сервис с ML моделью для семантического поиска ответов

### Основные возможности

✅ Семантический поиск по базе вопросов/ответов  
✅ Векторные эмбеддинги с моделью `ai-forever/FRIDA`  
✅ Хранение данных в Qdrant (векторная БД)  
✅ gRPC для быстрой межсервисной коммуникации  
✅ Graceful degradation (бот работает даже при недоступности ML)  

---

## 🏗️ Архитектура

```
arasaka/
├── max-bot/              # MAX Bot микросервис
│   ├── bot_main.py       # Точка входа
│   ├── handlers.py       # Обработчики команд/сообщений
│   ├── grpc_client.py    # gRPC клиент для ML Service
│   ├── config.py         # Конфигурация бота
│   └── requirements.txt  # Зависимости бота
│
├── ml-service/           # ML Service микросервис
│   ├── main.py           # Точка входа (gRPC сервер)
│   ├── config/           # Конфигурация
│   ├── domain/           # Бизнес-логика
│   ├── infrastructure/   # Техническая реализация
│   │   ├── grpc/         # gRPC server
│   │   ├── db/           # Qdrant
│   │   └── ml/           # ML модели
│   ├── tools/            # Утилиты (загрузка данных)
│   └── requirements.txt  # Зависимости ML
│
├── shared/               # Общие компоненты
│   └── proto/            # gRPC proto файлы
│       ├── arasaka.proto
│       ├── arasaka_pb2.py
│       └── arasaka_pb2_grpc.py
│
├── docker-compose.yml    # Qdrant + ML Service
└── .env                  # Переменные окружения
```

### Поток данных

```
Пользователь → MAX Bot → gRPC (8001) → ML Service → Qdrant → ML Service → MAX Bot → Пользователь
```

---

## 💻 Требования

### Обязательно

- **Python** 3.11+ (для ML Service) / 3.13+ (для MAX Bot)
- **Docker** + **Docker Compose** (для Qdrant и ML Service)
- **Git** (для клонирования репозитория)

### Рекомендуется

- 4+ GB RAM (для ML модели)
- 5+ GB свободного места (модель + данные)

---

## 🚀 Быстрый старт

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd arasaka
```

### Шаг 2: Настройка окружения

Создайте `.env` файл из примера:

```bash
cp env.example .env
```

Откройте `.env` и укажите **обязательные** параметры:

```bash
# MAX Bot токен (получите на https://auth.max.ru)
MAX_BOT_TOKEN=your_bot_token_here

# Остальные параметры можно оставить по умолчанию
QDRANT_HOST=qdrant
QDRANT_PORT=6333
ML_SERVICE_HOST=localhost
ML_SERVICE_PORT=8001
```

### Шаг 3: Запуск ML Service и Qdrant

```bash
# Запуск контейнеров
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f ml-service
```

**Важно:** При первом запуске модель `ai-forever/FRIDA` будет скачана (~2GB), это может занять 5-10 минут.

### Шаг 4: Загрузка данных в Qdrant

```bash
# Загрузить данные из CSV в векторную БД
docker-compose exec ml-service python ml-service/tools/fill_qdrant.py
```

Вы увидите:

```
✓ Loaded 500 questions and answers
✓ Created Qdrant collection 'arasaka_qa'
✓ Indexed 500 documents
```

### Шаг 5: Установка зависимостей

**Вариант А: Установить все зависимости (рекомендуется для разработки)**

```bash
# Создать виртуальное окружение (опционально)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
.\venv\Scripts\activate   # Windows

# Установить все зависимости сразу
pip install -r requirements.txt
```

**Вариант Б: Установить только зависимости MAX Bot (для продакшена)**

```bash
pip install -r max-bot/requirements.txt
```

**Примечание:** Общий `requirements.txt` включает зависимости обоих сервисов. Для продакшена можно устанавливать только нужные зависимости из `max-bot/requirements.txt` или `ml-service/requirements.txt`.

### Шаг 6: Запуск MAX Bot

```bash
python max-bot/bot_main.py
```

Вы увидите:

```
INFO - Starting MAX Bot...
INFO - gRPC client initialized: localhost:8001
INFO - Polling started...
```

### Шаг 7: Тестирование

Откройте мессенджер MAX и:

1. Найдите вашего бота по токену
2. Отправьте `/start` - получите приветствие
3. Задайте вопрос: *"Как поступить в университет?"*
4. Получите ответ из базы знаний

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```bash
# === MAX Bot ===
MAX_BOT_TOKEN=your_token_here
MAX_API_URL=https://messenger-api.vk.dev
MAX_POLLING_TIMEOUT=25
MAX_POLLING_LIMIT=100

# === ML Service ===
ML_SERVICE_HOST=localhost
ML_SERVICE_PORT=8001
ML_SERVICE_TIMEOUT=10.0

# === Qdrant ===
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=arasaka_qa

# === ML Model ===
MODEL_NAME=ai-forever/FRIDA
EMBEDDING_DIM=1024
```

### Настройка поиска

В `ml-service/config/config.py`:

```python
search_limit: int = 5              # Количество результатов
score_threshold: float = 0.3       # Порог релевантности (0.0-1.0)
```

---

## 🎮 Использование

### MAX Bot команды

- `/start` - Начать работу с ботом
- `/help` - Справка по использованию
- `/info` - Информация о системе

### Поиск ответов

Просто напишите вопрос в чат:

```
Вопрос: Какие документы нужны для поступления?
Ответ: Для поступления необходимы: паспорт, аттестат...
```

### gRPC API (для разработчиков)

#### Поиск ответов

```python
import grpc
from shared.proto import arasaka_pb2, arasaka_pb2_grpc

channel = grpc.insecure_channel('localhost:8001')
stub = arasaka_pb2_grpc.ArasakaServiceStub(channel)

request = arasaka_pb2.SearchRequest(
    query="как поступить?",
    limit=5,
    score_threshold=0.3
)

response = stub.SearchAnswers(request)
for result in response.results:
    print(f"Answer: {result.answer} (score: {result.score})")
```

#### Health Check

```python
request = arasaka_pb2.HealthRequest()
response = stub.HealthCheck(request)
print(f"Status: {response.status}")
```

### Тестирование ML Service

```bash
# Прямой тест gRPC API
python test_search.py
```

---

## 🛠️ Разработка

### Структура проектов

- **MAX Bot**: легковесный, только `umaxbot` и `grpcio`
- **ML Service**: полный ML стек (`torch`, `transformers`, `qdrant-client`)
- **Shared**: общие proto файлы для обоих сервисов

### Управление зависимостями

Проект использует три файла `requirements.txt`:

- **`requirements.txt`** (корень) - все зависимости для разработки
  ```bash
  pip install -r requirements.txt  # Установить все
  ```

- **`max-bot/requirements.txt`** - только зависимости бота
  ```bash
  pip install -r max-bot/requirements.txt  # Только бот
  ```

- **`ml-service/requirements.txt`** - только зависимости ML сервиса
  ```bash
  pip install -r ml-service/requirements.txt  # Только ML
  ```

**Рекомендация:** Для разработки используйте общий `requirements.txt`. Для продакшена устанавливайте только нужные зависимости.

### Обновление proto файлов

```bash
cd shared/proto
python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. arasaka.proto
```

### Локальная разработка ML Service

```bash
# Установить зависимости (если еще не установлены)
pip install -r requirements.txt  # Все зависимости
# или
pip install -r ml-service/requirements.txt  # Только ML Service

# Запустить только Qdrant
docker-compose up -d qdrant

# Запустить ML Service локально
python ml-service/main.py
```

### Добавление данных

1. Поместите CSV файлы в `ml-service/data/`:
   - `Questions__*.csv` (колонки: `id`, `question_text`)
   - `Answers__*.csv` (колонки: `id`, `answer_text`, `question_id`)

2. Запустите скрипт загрузки:
   ```bash
   python ml-service/tools/fill_qdrant.py
   ```

---

## 🔧 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**

```bash
# 1. Проверьте токен в .env
cat .env | grep MAX_BOT_TOKEN

# 2. Проверьте логи бота
python max-bot/bot_main.py  # смотрите на ошибки

# 3. Проверьте ML Service
docker-compose logs ml-service
```

### Проблема: ML Service не отвечает

**Решение:**

```bash
# 1. Проверьте статус контейнеров
docker-compose ps

# 2. Перезапустите ML Service
docker-compose restart ml-service

# 3. Проверьте порты
netstat -an | grep 8001
```

### Проблема: Модель не загружается

**Решение:**

```bash
# 1. Очистите кеш
docker-compose down -v
docker volume prune

# 2. Запустите заново
docker-compose up -d

# 3. Проверьте место на диске
df -h
```

### Проблема: "No matches found"

**Решение:**

```bash
# 1. Убедитесь, что данные загружены
docker-compose exec ml-service python ml-service/tools/fill_qdrant.py

# 2. Проверьте Qdrant
curl http://localhost:6333/collections/arasaka_qa

# 3. Понизьте score_threshold в config.py (например, до 0.0)
```

---

## 📚 Дополнительно

### Документация компонентов

- [MAX Bot README](max-bot/README.md) - подробнее о боте
- [ML Service README](ml-service/README.md) - подробнее о ML сервисе
- [Proto Documentation](shared/proto/README.md) - gRPC контракты

### Полезные ссылки

- [MAX API Documentation](https://dev.vk.com/ru/max/overview)
- [Qdrant Documentation](https://qdrant.tech/documentation/)
- [gRPC Python Guide](https://grpc.io/docs/languages/python/)

---

## 📝 Лицензия

Проект разработан для образовательных целей.
